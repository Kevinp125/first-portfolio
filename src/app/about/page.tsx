import Header from "../(components)/Header";

export default function app() {
  return (
    <div className = "relative  min-h-screen flex flex-col items-center ml-36"> <Header header = "About" icon = "About"/></div>

  );
}